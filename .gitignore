# ── Rust ──────────────────────────────────────────────────────────────────
/target
**/*.rs.bk
*.pdb
# NOTE: Cargo.lock IS committed — passman is an application, not a library
# (Rust convention: lockfiles are committed for applications).

# ── Android / Gradle (the Kotlin shim lands under android/ in Task 8) ──────
android/.gradle/
android/build/
android/app/build/
android/captures/
android/.cxx/
android/local.properties
*.iml
.idea/

# ── Editors ───────────────────────────────────────────────────────────────
*.swp
*.swo
*~
.vscode/

# ── OS cruft ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Secrets & signing material — NEVER commit (this is a password manager) ──
# Release/app signing keys and any crypto material. A *legitimate* test fixture
# with one of these extensions must be force-added (`git add -f <path>`).
*.jks
*.keystore
*.pem
*.key
*.p12
*.pfx
.env
.env.*

# ── Local tool output (e.g. a redirected `cargo check` scratch dir) ────────
cargocheck.*
/crates/passman-uniffi/bindings/
/crates/passman-uniffi/jniLibs/
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/app/src/main/jniLibs/
android/app/src/main/kotlin/uniffi/
android/.kotlin/

# cargo-fuzz build artifacts (the fuzz harness Cargo.toml + fuzz_targets/ +
# the committed seed corpus/ stay tracked). The corpus dir is intentionally NOT
# ignored: it ships a small seed corpus so libFuzzer starts from real inputs.
# cargo-fuzz also writes newly-discovered inputs into corpus/ at runtime — those
# appear as untracked and should not be committed (only meaningful seeds are).
crates/*/fuzz/target/
crates/*/fuzz/artifacts/
crates/*/fuzz/coverage/
crates/*/fuzz/Cargo.lock

# ── Build artifacts: never commit APKs (release/debug) ─────────────────────
*.apk
